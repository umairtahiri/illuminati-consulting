import React from "react";
import { Input } from "antd";

import "./styles.scss";

const IlluminatiInput = ({ placeholder, style }) => {
  return <Input placeholder={placeholder} style={style} />;
};

export default IlluminatiInput;
